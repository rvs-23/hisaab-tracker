# Grayscale base + two accents (see design.md): teal = actuals/current,
# mulberry = planned/projected. No other colors.
[theme]
base = "light"
primaryColor = "#0F766E"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f4f4f4"
textColor = "#1f1f1f"
font = "sans-serif"
